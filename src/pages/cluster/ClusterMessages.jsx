import LiveMessages from "../../components/LiveMessages";

export default function ClusterMessages() {
  return <LiveMessages subtitle="Professor to cluster professor conversation history and replies." />;
}
