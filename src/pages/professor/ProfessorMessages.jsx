import LiveMessages from "../../components/LiveMessages";

export default function ProfessorMessages() {
  return (
    <section className="professor-messages-page">
      <LiveMessages subtitle="Message cluster professors, admins, and enrolled students." />
    </section>
  );
}
